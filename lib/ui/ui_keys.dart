import 'package:flutter/widgets.dart';

/// Canonical keys used by tests and the runtime widgets.
class UIKeys {
  static const form = ValueKey('auth.form');
  static const email = ValueKey('auth.email');
  static const password = ValueKey('auth.password');
  static const signIn = ValueKey('auth.signIn');
  static const forgot = ValueKey('auth.forgot');
  static const create = ValueKey('auth.create');
}
